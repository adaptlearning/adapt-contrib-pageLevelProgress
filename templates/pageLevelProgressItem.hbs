<button
    class="base pagelevelprogress-item-title clearfix drawer-item-open{{#unless _isVisible}} disabled{{/unless}} js-indicator"
    data-pagelevelprogress-id="{{_id}}"
    aria-label="{{#unless _isVisible}}{{_globals._accessibility._ariaLabels.locked}}. {{/unless}}{{compile title}} {{#if _isComplete}}{{_globals._accessibility._ariaLabels.complete}}{{else}}{{_globals._accessibility._ariaLabels.incomplete}}{{/if}}">
    <div class="pagelevelprogress-item-title-inner">
    {{{compile title}}}
    </div>
    {{#if _isOptional}}
        <div class="page-level-progress-item-optional-text">
        {{_globals._extensions._pageLevelProgress.optionalContent}}
        </div>
    {{/if}}
</button>