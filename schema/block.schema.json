{
  "$anchor": "pageLevelProgress-block",
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "type": "object",
  "$merge": {
    "source": {
      "$ref": "block"
    },
    "with": {
      "properties": {
        "_pageLevelProgress": {
          "type": "object",
          "title": "Page Level Progress",
          "default": {},
          "properties": {
            "_isEnabled": {
              "type": "boolean",
              "title": "Is enabled",
              "description": "Controls whether the Page Level Progress extension is used on this block.",
              "default": false
            },
            "_isCompletionIndicatorEnabled": {
              "type": "boolean",
              "title": "Show block completion indicator",
              "description": "Controls whether the completion indicator shows next to the block title.",
              "default": false
            }
          }
        }
      }
    }
  }
}
